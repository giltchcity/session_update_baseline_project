# Experiment Records

Only one compact canonical experiment record is kept active:

```text
current/
```

It contains reports, configs, metrics, object-memory summaries, manifests, and
selected figures. It does not contain generated map binaries or per-frame mesh
sequences. Superseded experiments are described in Git history rather than
accumulating as `roundXX` directories.
